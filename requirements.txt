# Core RAG & Vector Store
langchain==0.3.25
langchain-community==0.3.23
faiss-cpu==1.9.0.post1
sentence-transformers==3.4.1

# LLM APIs
groq==0.23.1
google-generativeai==0.8.5

# Document Parsing
pypdf==5.4.0
python-docx==1.1.2
pyyaml==6.0.2

# Frontend
streamlit==1.44.1

# Evaluation
pytest==8.3.5
pytest-cov==6.1.0


# Evaluation — LLM-based (DeepEval)
# Install separately if you want full LLM-judge evaluation:
#   pip install deepeval
# Without it, the heuristic fallback runs automatically.
deepeval>=0.21.0

# Sparse Retrieval (BM25 is implemented from scratch — no extra dep needed)
# For rank_bm25 as an alternative: pip install rank-bm25



# Utilities
python-dotenv==1.1.0